#include "aeonterracpp/core/vector_types.h"

// This file is mostly empty as vector_types.h is mostly a header-only library
// It's included in CMakeLists.txt for future expansion

namespace aeonterracpp {
    // If we need any non-inline implementations, they would go here
}